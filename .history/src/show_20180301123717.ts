export default class Show {
    _id: number;
    title: string;
    description: string;
    price;
    eventType: string;
    host: string; // added feature - might be concerted to model type.
    map: string;
}